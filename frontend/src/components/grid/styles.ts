import styled from "styled-components";

export const StyledGrid = styled.header`
  padding: 85px 130px;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(241.47px, 1fr));
  grid-gap: 66px;
  background-color: white;
`;
